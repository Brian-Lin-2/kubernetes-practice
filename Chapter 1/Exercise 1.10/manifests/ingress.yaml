apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
   name: log-app-ing
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: log-app-svc
            port:
              number: 2500
